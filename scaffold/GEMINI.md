# GEMINI.md

Primary instructions for this workspace are in **`CLAUDE.md`** at the root. Load that file as your context for this session.

This file exists so that Gemini CLI — which auto-discovers `GEMINI.md` (not `AGENTS.md` or `CLAUDE.md`) walking up from the working directory to the project root — is pointed at the same memory system used by tools that read `CLAUDE.md` or `AGENTS.md`. One source of truth, three entry points.

If you're an AI assistant reading this: read `CLAUDE.md` now and follow the setup / session protocols described there.
